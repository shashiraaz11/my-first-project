name: All_collection_recovery

on:
  schedule:
    # IST 8:05 AM → UTC 2:35 AM
    - cron: '35 2 * * *'
    # IST 10:05 AM → UTC 4:35 AM
    - cron: '35 4 * * *'
    # IST 12:05 PM → UTC 6:35 AM
    - cron: '35 6 * * *'
    # IST 2:05 PM → UTC 8:35 AM
    - cron: '35 8 * * *'
    # IST 4:05 PM → UTC 10:35 AM
    - cron: '35 10 * * *'
    # IST 6:05 PM → UTC 12:35 PM
    - cron: '35 12 * * *'
    # IST 8:05 PM → UTC 14:35 PM
    - cron: '35 14 * * *'

  workflow_dispatch:

jobs:
  run-All_collection_recovery:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install gspread pandas google-auth

      - name: Run All_collection_recovery.py
        env:
          ACCOUNT_KEY_JSON: ${{ secrets.ACCOUNT_KEY_JSON }}
        run: |
          echo "🚀 Starting All_collection_recovery.py..."
          python All_collection_recovery.py
